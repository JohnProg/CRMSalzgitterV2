import { CbxFamilyDirective } from './cbx-family.directive';

describe('CbxFamilyDirective', () => {
  it('should create an instance', () => {
    const directive = new CbxFamilyDirective();
    expect(directive).toBeTruthy();
  });
});
